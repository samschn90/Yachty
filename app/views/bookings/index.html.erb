<h1>All Bookings</h1>
<ul>
  <% @bookings.each do |booking| %>
    <li><%= booking.yacht %></li>
    <li><%= booking.date %></li>
    <li><%= booking.user %></li>
    <%= button_to "Accept" %>
    <%= button_to "Decline" %>
  <% end %>
</ul>
